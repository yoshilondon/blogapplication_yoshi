<div class="container">
  <ul>
    <% @article.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>  
  <%= form_with(model: @article, url: articles_path, method: 'post', local: true) do |f| %>
  <%# どのmodelを更新するか: model, どこにリクエストを送るのか: article_path  %>
    <div>
      <%= f.label :title, 'タイトル' %>
    </div>
    <div>
      <%= f.text_field :title, class: 'text' %>
      <%# フォームを作るためのメソッド：フォームヘルパー %>
    </div>
    <div>
      <%= f.label :title, '内容' %>
    </div>
    <div>
      <%= f.text_area :content %>
    </div>
    <%= f.submit '保存', class: 'btn-primary' %>
  <% end %>
</div>